{
  "description": "v2 active partition without a partition block: missing block name (rule row 4)",
  "env": {},
  "error": {
    "kind": "invalid-config",
    "contains": [
      "partition",
      "missing"
    ]
  }
}

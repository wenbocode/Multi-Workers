# Project Log — FeatureMigrator

> 追加型 / 不可变时间线。由 advance_phase.py done 追加。

| Completed | Commit | Key | 核心问题 | Supersedes |
|-----------|--------|-----|---------|------------|
| 2026-09-09 16:59 | 6273b27f9 | mw-worker-timeout-convergence | Worker watchdog redo: activity idle detection + per-task budget + convergence checkpoint with PM escalation + deadline steer; 87/87 tests, live smoke x2, quality gate PASS | — |
| 2026-09-09 23:53 | 357296d89 | mw-dispatch-flow-fixes | — | — |
| 2026-09-10 19:27 | fceae6eaf | mw-widget-terminal-lifecycle | Widget 三区渲染 + ack 通道/sidecar + 终态 detail 取源 + TL;DR 双保险 + 孤儿行 reconcile；TS 121/121 + Python 365 + bundle 冒烟 + 38/38 质检（review S1~S3 已修复） | — |
| 2026-09-10 19:32 | 5e8ecf39d | goal-autopilot | T-18 wrap-up: QG PASS (72/72), 18/18 tasks done | — |
| 2026-09-11 00:19 | 04bc69616 | ai-baseline-repair | AI baseline drift repair: tsgo 26->0 + check green + ai suite 838 passed (root fix = upstream generator mirror block); attribution B=58/A'=26/A=0 with user decisions recorded; prevention docs in README+AGENTS.md; QG 38/38 PASS | — |
| 2026-09-11 17:19 | 5fa74a900 | autopilot-monitor | — | — |
| 2026-09-17 17:00 | 477d8ace4 | mw-dual-workspace | 质检 PASS（用户确认接受 4 项 ⚠️ 欠债）；quality-gate-report-2026-09-11.md + achieved.md 落盘；check 0/0/0、回归基线 new-failures=0、audit_phase PASS | — |
| 2026-09-19 13:59 | 67e70a8c8 | mw-target-partition | mw-target-partition 完成：target.yml 单文件 v2（active 键 + dual/partition 模式块）新增 partition 模式——大项目子目标独立分片（parent 只读 + 独立分片根 = worker cwd + 命名 roots）；mw partition set/show/clear/on/off 命令族 + v1 一次性迁移（.bak）；/mw partition TS 转发；profile v2 注入（模式行+切换整体替换）+ launcher 撕裂校验 fail-closed；doctor partition-only 键 + 指纹缓存。改动面：mw_common.py/mw.py/launcher.py/autopilot-dispatch.py + target-config/task-dispatcher/ui-bridge/mw-runner + dist bundle；测试 Py 660 / TS 377 全绿、S0 golden 基线逐字节 MATCH（v1 零回归）、规则表 12 行 112 参数 case 双侧 parity、真实 spawn E2E（cwd=分片根+v2 注入+config-torn 负路径）。质检两轮（FAIL 4 BLOCKER→处置→复核 PASS）。 | — |
| 2026-09-19 18:41 | 67e70a8c8 | mw-worker-tree-kill | worker-mode.ts 4 个退出位点 + exit 安全网调用 killTrackedDetachedChildren（taskkill /T / kill(-pgid)），修复 watchdog 杀 worker 后 bash 子树孤儿化事故；+2 测试文件（5 单测 + 真实树 live），全链 mw 派发 smoke 零孤儿，质检 21/21 通过；dist 重建 + serve 已重启生效。 | — |
| 2026-09-20 16:07 | 44f30c338 | mw-partition-parent-extended | partition parent 语义修正：只读上下文 → 扩展可写工作区。Py 派发展开并入 parent root + TS worker 侧 profile 解析 union（双层，空 scope/deny-only 不回退，deny 先于包含性，写路径零拦截回归锁定），标签与 mw.py 文案去 read-only，golden parity 保持。触达 6 源文件 + 5 测试文件（dual/single 零修改）；Py 678/TS 44+188 全绿；PM 自查 + worker 独立质检双 PASS。取代 mw-target-partition spec §1.1/§1.4/§4 的只读定性（旧 key 归档不回改）。 | — |
| 2026-09-20 20:11 | a395b0af2 | mw-provider-routing | zai-coding-cn 直连 provider 路由落地：providers.json 凭证链（env→auth.json）+ 无 port 路由、launcher 第 5 个直连分支（zai/ 前缀 → pi --provider zai-coding-cn，显式 model 必填，timi 默认不动）、TS/Py prefix map 双侧同步 + bundle/dist 重建。影响面：packages/multi-workers（providers.json/mw_common/launcher/测试）、coding-agent dispatch-models + dist。验证：全量 pytest 689 passed 0 failed、vitest parity 158 passed、npm run check 绿、e2e_real 真实 glm-5.3 往返 exit 0；质检门禁 28 问全充分。提交 a395b0af2 + 57ca0e166。 | — |
| 2026-09-20 20:52 | a395b0af2 | mw-dispatch-role-escape | 派工契约补齐：dispatch_worker//worker 可声明 type（pi 首次可达 review/research role 与白名单）+ 覆盖 role 默认值必须带 model_reason 并落盘 task.md model-reason + 派发期 pi 模型 id 校验（含 dispatch.yml BOM 容错）；launcher 增 model-override 证据行。影响面：agent-team-loop 扩展（dispatch-models/ui-bridge/pm-orchestrator）+ launcher.py + bundle/dist 重建；TS 168/168、Py 692 passed、check 0/0/0、安装后 bundle live smoke 通过 | — |
| 2026-09-21 22:17 | 08ace623a | mw-ue-toolchain | 新增 mw ue-toolchain 三动词（run 执行留证+合成判定 / targets 目标现查 / hash EOL 归一化）+ mw_common 三纯函数 + 实践指南入仓 + 定义域六处钉死（dual UE / MSVC-UBT）；影响面仅 packages/multi-workers，722 测试过、npm check 绿。本 key 为野生实施事后补登记（流程偏差已记录）。 | — |
| 2026-09-22 00:49 | 08ace623a | mw-implementation-gate | 实施准入门禁机械化：tool_call 硬门（write/edit 代码路径 + bash 写目标收窄）+ 三条件放行（claim/worker env/新鲜 mini-spec）+ 全模式审计；4 worker 派发协作完成，suite 9/9 真实派发层验证，真缺陷（Windows 反斜杠词法漏拦）已修并登记 P-004；框架 SKILL.md 触发词扩面 e9360db。生效需 mw setup --build + 窗口重启。 | — |
| 2026-09-22 16:38 | 08ace623a | mw-autopilot-advance-root | 修复 autopilot phase-advance 通道跨项目根错位：framework root 定位改为 cwd 向上专扫 .agenticdoc 优先（advance_phase/update_index/migrate_patterns/verify_project_memory + detect_root CLI），mw 侧按 detect_root PROJECT_ROOT 做候选一致性校验并 fail-loud，两处子进程与 pi 扩展加 -X utf8。影响面：packages/multi-workers/autopilot/advance.py、agent-team-loop agent-scripts.ts、AgenticTask framework d7004d0；726 mw pytest + 168 vitest + check 0/0/0，e2e 验证项目外 framework 不再写错 .agenticdoc。 | — |
| 2026-09-22 18:56 | d106bcfb2 | mw-rag-integration | 可配置 RAG 接入落地：两层 YAML 逐字段合并 + target.yml 启用集/角色/阶段要求，六工具面（rag_chat 仅 rag-research）+ overcode-v1 逻辑名→线上工具名映射 + 五层降级/熔断/预算/心跳 + rag_call 等证据行与只读 mw rag audit + task.md phase: 阶段轴 + mw rag list|probe|audit|sync 与 skill 同步；未启用项目结构上零影响。验证：28/28 VC 实测（TS rag 97、autopilot 86、extensions 405、Python 802 passed，180s 慢调用 L2 实跑，npm run check exit 0，test_autopilot_l0.py 零 diff）；质检门禁 0 项未通过、2 项验证欠债（真实 rag-mcp 联调）。 | — |
| 2026-09-22 21:44 | d106bcfb2 | mw-parallel-protocol | PM 模式新增 before_agent_start 常驻「并行优先协议」注入：spec/design 调研默认批量派 research worker（一 RQ 一 note）、编码按文件边界并行、相位文档 PM 串行；静态追加式、标记幂等、worker 模式不受影响（pm-orchestrator + 2 单测 + changelog，npm run check 与 170 用例回归通过） | — |
| 2026-09-22 22:01 | d106bcfb2 | mw-rag-integration-fix | 闭合上位复核 5 组缺口 F-1~F-5：RAG 角色/阶段默认接入运行时（新增 resolveDefaults 作为每语言唯一解析入口，被 callRag 与 renderRagBlock 共用，删除 resolveForRole/Phase；默认优先级统一为"声明值：显式 > 角色 > 阶段 > default_server"；Python rewrite 兜底对齐"研究角色或阶段"；未登记 type 的 role 回落三处对齐 coding）；补 fixture 级 VC-008 与不可达 server 断言；VC-018 证据层级如实降为 L1；README/退出码/probe help 以 argparse 为唯一真源并删除死变量 MW_RAG_ENABLED。全套件零回归（TS rag 105|1 skipped、Python 805、add 488、npm run check exit 0），golden 字节锁不变，3 条反例试验全部证伪并还原，质检 23 项 20 充分/3 部分/0 不成立。 | — |
| 2026-09-23 00:21 | d106bcfb2 | mw-autopilot-stall-feedback | autopilot 停滞治理：conductor 对 advance 失败分类 + 连击阈值（advance_stall_ticks）落既有 stalled 四件套；stalled 门禁 approve 从 no-op 变为复位+各受管回路放宽一轮（消费由单调轮次计数保证）；L3 worker 崩溃判 no-verdict 而非 below；PM 窗口监控面板新增 autopilot 分节（tick 新鲜度/槽位/每 key 相位与停滞连击/门禁处置命令）并在 enabled 项目自动显示。影响面：packages/multi-workers/autopilot/{conductor,timeline,config}.py、test_autopilot_*（820 passed）、packages/coding-agent agent-team-loop/autopilot/{monitor,status-model,console}.ts + pm/pm-orchestrator.ts（session_start 4 行追加）、两个 CHANGELOG + README；现场 E2Feature K2 经 approve gate-0002 正式闭环（verify->done exit=0，K3 解锁）。未做：dist 重建与扩展目视验证（等并发 key 提交，AC-011 未决）、未 commit。 | — |
| 2026-09-23 12:02 | 0c7754086 | mw-rag-config-guide | 新增 `mw rag init`（零交互生成三份逐字段注释的 RAG 配置模板：`.mw/rag-servers.yml`、`target.yml` 的 `rag:` 段、`.mw/rag-roots.json`）+ 手册 `packages/multi-workers/docs/rag-config-guide.md`（519 行，逐字段表 25 行 4 列、四类可复制示例、命令/退出码、五层判据、失败 signature、已知坑）+ `test_rag_init.py`(12)/`test_rag_docs.py`(5)（手册模板块与 `--print` 字节 parity、字段表 == 校验器字段集）；影响面：仅 `packages/multi-workers`（新模块 + `mw.py` 子命令 + 文档 + README/CHANGELOG 指向），不改 `rag/*.ts` 与 `mw_common.py` 语义，未启用 RAG 的项目零影响（默认 `enabled: []`）；零回归 839 passed、`npm run check` exit 0；遗留 R-1~R-5（skill.dir 跨语言差异、pi 窗口内无 `/mw rag`、真实服务联调等）。 | — |
| 2026-09-23 12:52 | 0c7754086 | mw-rag-window-parity | mw-rag-window-parity: pi 窗口 RAG 面 + TS 加载器跨语言 parity 收口 —— 新增 `/mw rag <list|probe|audit|sync|init>` 薄封装（exit 0/1/2→info/warning/error、>30 行截断、未知子命令不调 CLI、真 spawn 断言 argv 含 `--project=<控制工作区>`）与 `/mw doctor` 的 RAG 行（与 `mw.py doctor` 逐字一致、同门控 `exists or enabled`）；TS `skill.dir` 接受缺省/`null`（= 控制工作区根，fingerprint 与 Python 逐字节相等）并对 `mcp.url`/`mcp.token_env`/`skill.dir`/`skill.cli_entry` 取 `.trim()` 镜像 Python `.strip()`；Python 零改动、fixture/golden 零 diff。影响面：coding-agent 扩展（rag/config.ts、rag/tools.ts、shared/mw-runner.ts、pm/ui-bridge.ts）+ 3 个测试文件 + CHANGELOG；质检 ⚠️ 有条件通过（8 欠债中 4 项已闭合），遗留 R-6（path_roots_file padding 语义）/R-7（pi 命令级接线测试）/R-8（无 L2 baseline 目录）/R-9（真实服务与 TUI 人工验证未做）。 | — |
| 2026-09-23 17:05 | 1e37cf9cf | mw-worker-progress-persist | 只读角色 worker 检查点证据由框架落盘（progress.md 机器行）+ 新增 basename-only 窄工具 worker_file + steer 按角色分化；改 worker/worker-mode.ts、worker/output-writer.ts、rag/tools.ts、pm/pm-orchestrator.ts 与配套测试/CHANGELOG/文档，零 Python 行为改动 | — |
| 2026-09-23 17:14 | 1e37cf9cf | mw-task-scope-isolation | worker 任务面按窗口隔离：list_tasks 默认 scope:"mine"（watched key ∪ 本进程派发）+ owner 前缀行 + scope key/all，ack_worker_result 与 /mw ack 只作用于本窗口（跨 key 拒绝并指引 /pm-key switch，侧车不再被误写）；零 Python/文件格式改动，3 新用例 + 173 回归 + npm run check exit 0 | — |
| 2026-09-23 19:25 | ec93ac5a6 | mw-worker-visibility-gate | worker 可见性与派发门禁相位分层：门禁按 owner key 相位分层（SPEC 只校验 spec 侧四项、DESIGN 起追加 design 侧两项，三处调用点统一接线），底栏面板新增跨 key 聚合提示行（含 risk=high 计数，不传参数时输出与改造前逐行相同），TS claim/takeover 把索引行同一 host:pid 同步进 pm-state.md（单行替换 / 缺行插行 / 换行守卫 / 原子写）。影响面：packages/coding-agent agent-team-loop shared/{phase-docs,pm-state-claim}.ts + pm/{ui-bridge,pm-orchestrator}.ts + 4 个新测试文件 + 1 处既有断言（D-110）+ 两包文档与 _pitfalls P-011；零 Python、未改 dist。质检 13/13 AC 有证据（31 充分 / 1 有条件 / 0 无证据），3 变异反例双向成立，回归 204/204 + npm run check EXIT=0。 | — |
| 2026-09-24 16:54 | 6254783b6 | mw-crosskey-risk-escalation | 跨 key 分化升级投递：monitor 的 risk 升级归属判定从「仅 watched key」扩到「watched key ∪ 本窗口派发」（pm-orchestrator 的 startWorkerPollLoop，2 行条件 + 文案），异地 alert 附加 owner key；低风险抑制、escalated 去重、triggerTurn 与 watched key 文本逐字节不变，别窗口 worker 仍静默。影响面：packages/coding-agent agent-team-loop pm/pm-orchestrator.ts + 3 个新 monitor 用例（agent-team-loop.test.ts 纯追加 159 行/0 删除）+ CHANGELOG；零 Python、未改 ui-bridge/shared/dist。质检 4/4 AC 有证据（11 充分 / 1 有条件 / 0 无证据），3 变异反例双向成立，回归 183/183 + npm run check EXIT=0。 | — |

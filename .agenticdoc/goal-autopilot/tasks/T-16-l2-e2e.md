# Task T-16: L2 E2E（计时 / 隔离 / 并发压测）

## 基本信息
- Stage: 4
- 代码状态: 代码完成（test_autopilot_e2e.py 5 测试 8 组 [VERIFY]；pytest.ini e2e_l2 标记）
- 验证状态: 验证通过（默认窗口全量跑 5/5，全部 pass=true；证据 evidence/runs/l2-e2e-2026-09-10.log）
- 负责 Agent: PM 窗口（真进程链验证，直执）
- ac_refs: [AC-001, AC-002, AC-004, AC-012, AC-016, AC-019, AC-020, AC-025]
- vc_refs: [VC-002, VC-004, VC-006, VC-014, VC-018, VC-021, VC-022, VC-027]
- pattern_refs: []

## 描述
新文件 `packages/multi-workers/test_autopilot_e2e.py`——脚本化 E2E，stub worker 完成器（确定性假 worker：写 output.md + exit 0；roadmap-writer stub 产合法 _roadmap.md；reviewer stub 产 meets 裁决节），真实拉起 conductor（必要时含 mw serve）与真实文件/锁/时间线。

计时类断言统一输出双值 `[VERIFY] VC-NNN: delay_ms=<d> interval_ms=<i> pass=true`（动态阈值，不用魔法常数）：

1. **VC-004 stage 门禁时延**：stage-confirm 回答文件落盘 → 该 stage 首个派发行出现，delay ≤ 2×interval
2. **VC-006 并行**：stage 激活 + 两个无依赖 key 就绪 → 两 key 首派发行均 <10s 且并存（parallel_dispatch=2）
3. **VC-002 roadmap 产物**：stub roadmap-writer 产出 _roadmap.md → roadmap_check 全 stage 过（stages_valid=all）
4. **VC-018 gate 消费时延**：`/autopilot gate` 等价文件写入（或调 gate-writer 语义）→ conductor 读取并推进，delay ≤ interval；timeline gate-answered 与后续动作事件时差 ≤ interval
5. **VC-021 节拍**：conductor 存活 60s 观测窗 → timeline beat ≥12（默认 4s → 15）
6. **VC-027 enable 链路**：未启用项目 enable → mw status 显示 conductor 运行 + roadmap-writer 派发行 delay ≤ interval（act-then-sleep：serve 检测 + spawn + 首 tick）
7. **VC-014 启用隔离**：未启用项目 → conductor 进程不存在（无 .mw/conductor.pid 且 mw status 无 conductor 行）+ .agenticdoc 无 _autopilot 写入 + 现有手动套件结果与 T-01 baseline 一致
8. **VC-022 并发写压测**：conductor + 模拟人工写入方并发写 `_workers.parallel` 与 `_index.parallel` 60s → 全部行按各文件列数完整性解析（_workers 7|8 列容错、_index 7 列）且双方最终行集无丢失（并集相等）

注意：
- stub worker 经 providers.json/路由层接入（或最小化：直接替换 spawn 目标），不得触网、不得用真实 LLM
- 测试自身不修改被测产品代码；fixture 项目在临时目录构建
- 60s 窗测试可接受较长运行时间；CI 分层标记（L2 手动/夜间跑亦可，证据落盘为准）

## 输入
- 依赖文件: autopilot/ 全模块 + mw.py（serve/status）+ TS 侧产物（VC-018 若走 TS gate-writer 需 bundle 或直接文件写入等价验证）
- 依赖 Task: T-01（baseline）、T-09~T-15 全部
- AC 约束:
  > AC-001 / AC-002 / AC-004 / AC-012 / AC-016 / AC-019 / AC-020 / AC-025（全文见 spec §3；对应上述 8 组断言）
- 设计约束:
  > §2.2 性能指标：interval ≤5s；gate 消费 ≤1 interval；stage 首派发 ≤2 interval；并行 <10s
  > AC-020 erratum：列数按各文件实际（_index 7 / _workers 7|8 容错）

## 预期产出
- `packages/multi-workers/test_autopilot_e2e.py`（stub 完成器 + 8 组断言 + [VERIFY] 行输出）
- `evidence/runs/` E2E 运行记录（原样输出）
- 验证方式: `python packages/multi-workers/test_autopilot_e2e.py`，全部 [VERIFY] 行 pass=true
- 验证等级: Level 2

## 状态与证据

### 执行记录
| 轮次 | 时间 | 动作 | 结果摘要 |
|------|------|------|--------|
| 1 | 2026-09-10 20:20-21:10 | 读全链路事实（launcher spawn/resolve_cli PATH、_build_env 凭证链、serve 监护 1s 循环、advance_phase.py 门禁规则、detect_root 向上扫 .agenticdoc、_workers/_index 双锁协议、archive_stale 规则）→ 写 test_autopilot_e2e.py：真 conductor/launcher/serve 进程 + 框架副本内嵌临时项目（advance 真门禁）+ PATH 遮蔽 pi 假 worker（task.md type/loop 驱动；roadmap-writer 产合法提案、gen 各阶段产过门 artifact、reviewer 产 meets 裁决）+ fixture providers.json（MW_E2E_FAKE_KEY 环境凭证，precheck 密闭通过、零网络）+ 5 测试 8 组断言 | 快模式迷代 5 轮后默认窗口 5/5 全绿 |
| 2 | 2026-09-10 21:10 | 默认窗口全量跑（60s beat + 60s 压测）+ 默认套件回归 358 passed/6 deselected（e2e_l2 排除生效；含另一会话并行新增的 launcher 用例）+ pytest -m e2e_l2 --collect-only 5 collected | 证据落盘 |

### Error Fingerprint
| ID | 类型 | 文件:行 | 错误文本（原始）| 状态 |
|----|------|---------|---------------|------|
| E1 | 路径推导 | stub root=parents[4] | roadmap 写到了项目外（%TEMP%/.agenticdoc）——parents[0] 是父目录而非自身，多爬一级 | 已修：parents[3]；清理误写目录 |
| E2 | 语义误解 ×2 | stub roadmap key-status=pending / config poll_interval_sec=0.5 | roadmap_check 拒绝（key 状态仅 running/done/stalled/closed-legacy）/ config 校验 int ≥1 | 已修：key-status=running；sub-1s 走 conductor --poll-interval float 旗标 |
| E3 | **编辑工具铁律重犯** | test_autopilot_e2e.py 全文件 | 用 PowerShell `-replace` 改 pass_=true→**{"pass":...}，Get-Content 无 -Encoding UTF8 读成 GBK 再写回，全部 CJK 变 mojibake（含门禁关键字预期收益）| 已修：整文件 write 工具重写；教训：任何非 ASCII 文件的替换一律用 edit 工具 |
| E4 | 测试侧 ×3 | stress/serve 断言 | ①h-* 行无 task.md 被 archive_stale 归档（设计行为非丢失）→ 人工方写 task.md；②update_index 重写加表头 → 过滤 header/separator（注意 `|` 不在 [\s:-] 字符类、表头是 `| Key` 大写）；③_autopilot 存在断言错（config 本就住那）→ 改为仅 config.json 且无 timeline/gates/roadmap | 均已修 |

### 设计取舍备注
- **框架副本内嵌**：detect_root 从 PLATFORM_DIR 向上扫 .agenticdoc，外指框架会解析到本 repo——临时项目内拷贝 .agents/skills/agentic-task（1.4MB），advance_phase 真门禁全跑（6 次 advance 全过）
- **stub 接入方式 = PATH 遮蔽**（任务书“直接替换 spawn 目标”路线）：launcher `_resolve_cli` 走 shutil.which，bin 目录前置 PATH 即可注入 pi.cmd/pi shim，零产品代码修改；stub 行为由 task.md frontmatter type/loop 驱动（roadmap-writer 解析 prompt 键清单产合法提案；gen:spec/design 按框架门禁产 artifact+研究证据；reviewer 产 meets 裁决）
- **密闭凭证**：fixture providers.json 声明 pi 路由指向 MW_E2E_FAKE_KEY 环境变量，serve precheck/launcher _build_env 均过且零网络（stub 不读 env）
- **窗口缩放**：MW_E2E_FAST=1 按占空比等比缩窗（15s@1s ≙ 60s@4s，beats 均为 15，阈值 12 不变）；所有时延阈值 interval 相对（gate ≤1×、首派发 ≤2×、enable 链 ≤serve_poll+interval），无绝对魔法常数
- **VC-022 双方均按文档协议加锁**（workers.lock/index.lock，O_CREAT|O_EXCL）；人工行写真实 task.md 防 archive_stale 归档干扰（归档是设计生命周期非丢失）；断言并集无丢失+列数完整（_workers 7|8、_index 7）+无重复键行
- **VC-014 语义修正**：_autopilot/config.json 是配置本体（fixture 自建），隔离断言=仅 config.json、无 timeline/gates/roadmap、无 conductor.pid、status 无 conductor 行；“手动套件与 T-01 baseline 一致”由同 CI 会话跑的 L1 套件结构性覆盖（本测试只写临时目录）
- **e2e_l2 标记**：pytest.ini addopts 追加排除，默认套件不变速；验证入口=`python packages/multi-workers/test_autopilot_e2e.py`（任务书指定）或 `pytest -m e2e_l2`

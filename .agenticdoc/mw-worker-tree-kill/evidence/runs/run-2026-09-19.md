# Evidence Runs: mw-worker-tree-kill（2026-09-19）

## T-01 实现（worker-mode.ts 退出路径树杀）

- 变更：`packages/coding-agent/src/extensions/agent-team-loop/worker/worker-mode.ts`
  - `+import { killTrackedDetachedChildren } from "../../../utils/shell.ts"`（:4）
  - 4 个 `process.exit(1)` 位点前各 +1 行清理调用（:414 existsCheck、:441 refusal、:683 timeoutExit、:884 settled-catch）
  - `process.on("exit")` 安全网 hook 内 output 补写前 +1 行（:483）
- 机械不变量 grep（`rg -n 'process\.exit\(|killTrackedDetachedChildren'`）：
  ```
  4:import { killTrackedDetachedChildren } from "../../../utils/shell.ts";
  414:  killTrackedDetachedChildren();
  415:  process.exit(1);
  441:  killTrackedDetachedChildren();
  442:  process.exit(1);
  483:  killTrackedDetachedChildren();
  683:  killTrackedDetachedChildren();
  684:  process.exit(1);
  884:  killTrackedDetachedChildren();
  885:  process.exit(1);
  ```
  每个 `process.exit(` 前一行均为清理调用。✅
- `npm run check`：全绿（biome 1052 files / tsgo / shrinkwrap / install-lock / browser-smoke，无 fixes）。

## T-02 单测（agent-team-loop-worker-tree-kill.test.ts）

- 命令：`node ../../node_modules/vitest/dist/cli.js --run test/extensions/agent-team-loop-worker-tree-kill.test.ts`（包根）
- 结果：**5 passed (5)** — 264ms
  - AC-001 idle kill：kill 恰一次且 invocationCallOrder < exit(1) ✅
  - AC-002 wall kill（timeout: 2，60s checkpoint + 90s steer 后 120s wall）✅
  - AC-003 settled-catch（writePhaseFile mock 注错 "phase write boom"）✅
  - AC-004 exit listener 直调（listeners("exit") 增量，模拟硬崩溃）✅
  - AC-005 成功路径零 kill / 零 exit / [END] exit=0 ✅
- 原套件回归：`agent-team-loop.test.ts` **158 passed (158)** ✅

## T-03 live 测试 + 全量回归 + dist

- live：`agent-team-loop-worker-tree-kill-live.test.ts` **1 passed** — 真实进程树（2 个 tracked node sleeper，其一 spawn 孙 node 并落盘 pid）→ fake-timer 推进 60s 触发 idle watchdog → 真实 killTrackedDetachedChildren（win32 taskkill /F /T）→ 轮询探活：父×2 + 孙全部消亡（总耗时 2.11s，远低于 60s 上限）。[VERIFY] VC-006: orphan_count=0 ✅
- 3 文件合跑（tree-kill + live + 原 agent-team-loop）：**164 passed (164)**，无 worker 错误。
- 全量（包根 vitest --run，日志 tmp/coding-agent-vitest-full.log）：`69 failed | 2253 passed | 48 skipped`；23 个失败文件全部为已知 Windows 环境语义类别（config self-update / external-editor / model-registry shell 命令 / package-manager / fswatch 回归 / footer 短路径 / suspend / trust 等），数量 ≤ 已知基线 76，且与 agent-team-loop / tree-kill 面零交集（日志中两者零提及，vitest 默认 reporter 仅列失败文件）→ **new-failures=0**。root `test.sh`（git-bash）另见 tui 包 8.3 短路径类失败，同属预存环境问题，非本 key 触达面。
- `npm run check`（含两个新测试文件后复跑）：全绿。
- dist 重建：`npm run build`（tsgo -p tsconfig.build.json + copy-assets）→ `dist/extensions/agent-team-loop/worker/worker-mode.js` 18:09:34，`killTrackedDetachedChildren` 6 处（= 源码 6 处）。全局 pi（junction → 本仓库）`pi --version` / `pi --list-models` 冒烟正常。
- 部署剩余：`/mw restart`（serve PID 4448 运行 09-18 起 stale code；重启后新 worker 加载修复）。

## AC × VC 勾销

| AC | VC | 判定 | 证据 |
|----|----|------|------|
| AC-001 | VC-001 | ✅ | 单测用例 1（kill_before_exit=true, kill_calls=1） |
| AC-002 | VC-002 | ✅ | 单测用例 2（wall 分支同断言） |
| AC-003 | VC-003 | ✅ | 单测用例 3（catch 路径 kill 先于 exit） |
| AC-004 | VC-004 | ✅ | 单测用例 4（hook_kill_called=true） |
| AC-005 | VC-005 | ✅ | 单测用例 5（success_zero_kill=true, exit_code=0） |
| AC-006 | VC-006 | ✅ | live 用例（orphan_count=0, within=60s，实测 2.11s） |

## 全链 mw 派发 smoke（Q-D-2 补验，2026-09-19 18:36）

- 前置：`mw.py stop` → `start` 重启 serve（旧 4448 → 新 91432，launcher 65084）；mwtp-final-fixes-3 已确认 done（无 stale 行）。
- 派发：dispatch_worker `smoke-tree-kill-1`（type: coding, timeout: 2，指令 = 恰一次 `bash sleep 570` 前台挂起）→ 经 serve 队列 → launcher → worker pi 进程 pid=44776（新 dist 含修复）。
- 实测链路（trace.log）：10:36:27Z `bash sleep 570` 工具调用发起后挂起（本机 shell=PowerShell，sleep=Start-Sleep 别名，进程内挂起）；activity 冻结 → 10:37:16Z [CHECKPOINT] risk=high（机器判据对故意挂死 smoke 任务的预期误报，PM 判定继续等待）→ 10:38:16.412Z [TIMEOUT] wall: budget 120s exceeded (last activity 109s ago) → 10:38:16.414Z [END] exit=1 elapsed=120s tools=2。
- output.md 在 kill 前正确落盘（TL;DR/Summary/Exit Reason 完整，Exit Reason 含 wall timeout 与 checkpoint 摘要）。
- **孤儿检查**：kill 后 7s / 31s 两次探测，`Get-Process 44776` 为空（worker 已死）、`Win32_Process Where ParentProcessId=44776` 为空（挂起中的 shell 子树无任何残留）→ **全链 orphan_count=0** ✅。对照：若无修复，挂起 Start-Sleep（570s 预算）会存活至 18:45+。
- 基线：派发前 sleep.exe / bash.exe / 目标进程全零。
- 终态：row failed @ 10:38:20Z（wall timeout，预期结果），ack 完成。

结论：dispatch→serve→launcher→worker→挂起工具调用→wall watchdog→timeoutExit→write output→killTrackedDetachedChildren（taskkill /T）→exit 全链验证通过，Q-D-2 关闭。

## 独立 review 轮（2026-09-19 22:47 派发， 15:00 done）

- 派发：`review-mw-worker-tree-kill`（type: review，只读工具；静态审查，明确声明未代跑测试）。结论：needs-fix，**0 Critical / 1 Warning / 5 Suggestion**，代码与测试本体 LGTM。
- 处置（全部采纳）：
  - **W-1（阻塞）**：CHANGELOG [Unreleased] → Fixed 缺条目 → 已补（packages/coding-agent/CHANGELOG.md，与 worker harness 既有条目同风格）
  - **S-1**：exit hook 的 kill 包独立 try/catch（未来 kill 路径若引入抛错不致跳过后续兜底写盘）→ worker-mode.ts 已改
  - **S-3**：existsCheck/refusal 两个早期位点无用例 → 补 2 个（t-missing 断言 rejects+kill 先于 exit；t-refused 断言 [ERROR] Fail-closed 行 + output 拒绝文案 + kill 先于 exit）
  - **S-4**：expectKillBeforeExit 头注释补「exit 被 mock 故 hook 不触发，真实运行 hook 会二次 no-op 调用（幂等由 clear() 保证），该路径由 live 测试覆盖」说明
  - **S-2**：Windows 窄窗口（tracked 直接子进程在写盘与 taskkill 之间自然退出→孙进程孤儿，taskkill /T 无法从死根遍历；POSIX 无此窗口）→ 记入 achieved.md 遗留（归 Job Object 后续项）
  - review 行动项 2（代跑测试 + check）：已重跑——单测 7/7（新增 2）+ live 1/1 + 原套件 158/158 = **166/166**；`npm run check` EXIT=0（含 biome 1054 files 零 fix）
  - review 行动项 3（部署）：已重建双 dist——coding-agent/dist 23:07:24 + `mw build --install`（mw bundle 23:08:35 + 全局 ext 安装）；serve 无需重启（worker 每次 spawn 读新 dist）
- 环境插曲：root `npm run build` 在 packages/ai 失败（kimi-coding.ts 缺 models.ts——**另一会话在途的新 provider 工作**，非本 key 触达面，未触碰）；改用包内 `npm run build`（成功）+ `mw build`（esbuild bundle 不依赖 packages/ai），双 dist 均已含 S-1 结构（bundle 内 hook 为 `try { kill } catch {}`）。

# E2E: real partition-mode worker spawn (mw-target-partition BLOCKER-1)

Date: 2026-09-19 · Branch: dev/AgentTeam · Worker: mwtp-e2e-spawn
Scope: 真实 spawn E2E 冒烟（spec §0 括号项：spawn cwd = 分片根、task.md profile 含 parent/partition；on/off 已由 CLI E2E 覆盖）。补质检 FAIL 项：现有 E2E 只有 CLI 层，未覆盖真实 worker spawn 与 profile 注入。

结论（先行）：**真实 spawn 完成**。独立控制根 + 真实 v2 配置 + dist 产物真实注入 + 真实 launcher 进程 + 真实 pi worker（timi/glm-5.3，1 次模型调用，22s）全链路跑通；tear 拒绝路径同步验证。未修改任何 src/test 实现文件。

`[VERIFY] VC-006 + VC-007 + §0-E2E: spawn cwd=C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-partition, profile=v2-block, writeback=C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-spawn\.agenticdoc\`

## 布局（%TEMP% 下全新控制根，未触碰本仓库 .agenticdoc 与 ~/.pi/agent，P-002）

| 角色 | 路径 |
|---|---|
| 控制根 | `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-spawn` |
| 父项目根（只读上下文） | `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-parent` |
| 分片根（预期 worker cwd） | `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-partition` |
| SDK root | `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-sdk` |
| launcher 日志 | `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-logs\` |

## 步骤与 exit code

### 1. 真实 v2 配置（mw partition set）
```
python packages/multi-workers/mw.py partition set --project <控制根> --parent <父> --partition <分片> --root sdk=<sdk目录>
```
exit=0。`<控制根>\.agenticdoc\target.yml` 落盘，`active: partition`，partition 块含 parent/partition/roots.sdk；`mw partition show` exit=0 输出四根正确。

### 2. 真实 profile 注入（dist 产物）
node 脚本 import `packages/coding-agent/dist/extensions/agent-team-loop/pm/task-dispatcher.js`（S1~S3 已落盘的 bundle dist）的 `dispatchTask`（内部即 `injectWorkspaceProfile`），store 传 no-op 只保留注入副作用，对 E2E task.md 真实注入。exit=0。

注入后 task.md 全文（分片 cwd 探针，`<控制根>\.agenticdoc\mwtp-e2e-spawn\workers\mwtp-e2e-cwd-probe\task.md`）：

```markdown
type: coding
timeout: 5

你是 mw-target-partition E2E 冒烟的 cwd 探针 worker。只做以下两件事，然后立即结束：

1. 在当前工作目录运行 pwd（当前 shell 是 PowerShell，直接运行 pwd 即可）。把 pwd 的输出中出现的当前目录绝对路径原样（不改动、不总结、不翻译）写入这个文件的单独一行：
   C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-spawn\.agenticdoc\mwtp-e2e-spawn\workers\mwtp-e2e-cwd-probe\cwd-evidence.txt

2. 写完后立即结束，最终回复只输出一行：cwd 记录完成 + 你看到的目录路径。

不要做任何其他工作；不要读取或修改其他文件；不要探索目录树。

<!-- mw-profile: v2 -->
[mw] mode: partition
[mw] Workspace profile (target.yml essentials, injected at dispatch;
full file: C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-spawn\.agenticdoc\target.yml)
Control workspace: C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-spawn
Parent root: C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-parent
Partition root (worker cwd): C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-partition
Named roots:
- sdk: C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-sdk
```

v2 标记 + `[mw] mode: partition` 行 + Parent/Partition/roots 行齐全（PROFILE_MARK_V2 块）。

### 3. 队列与索引（本仓库 mw 队列形态）
- `<控制根>\.agenticdoc\_workers.parallel`（8 列：task_key|status|cli|provider|task_path|dispatched_at|updated_at|model）：
  `mwtp-e2e-cwd-probe | pending | pi | timi | <task.md 绝对路径> | <ts> | <ts> |`（model 空 → launcher 链 task.md > dispatch.yml > window-model 均缺 → per-cli 默认 glm-5.3）
- `<控制根>\.agenticdoc\_index.parallel`：镜像本仓库格式，owner key `mwtp-e2e-spawn` 一行（launcher/worker 不读它，仅保真仓库形态）

### 4. 真实 spawn（独立 launcher 进程）
```
python packages/multi-workers/launcher.py --project <控制根> --poll-interval=1 --pi-port=7011 --claude-port=7013
```
后台启动（Start-Process，stdout/stderr 重定向 mwtp-e2e-logs\）。端口 7011/7013 避开在跑服务 7001/7003（Python PID 63128）。本 E2E 走 pi|timi 直连路由，不经过 proxy 端口；端口 flag 仅按任务要求携带（对 timi 路由无效无冲突）。

时序（UTC，来自 trace.log）：
- 05:50:43 队列 pending→running（1s 内被捞取）
- 05:50:44 `[START] pid=75508` + `[START] ... type=coding phases=-` + `[MODEL] model=glm-5.3`
- 05:50:47 read task.md → 05:50:54 bash pwd → 05:51:01 write cwd-evidence.txt（[TOOL] 行各一，目标路径 trace 截断为 80 字符系 truncLine 设计行为）
- 05:51:06 `[END] exit=0 elapsed=22s tools=3`，队列 running→done
- worker.log：`[worker] done exit=0 elapsed=22s tools=3` + 末行回复 `cwd 记录完成 + C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-partition`

### 5. 断言结果

| 断言 | 结果 |
|---|---|
| task.md 注入块（v2 标记 + mode: partition + parent/partition/roots 行） | PASS（见上全文） |
| cwd-evidence.txt = 分片目录绝对路径 | PASS：内容恰为 `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-partition`（单行，57 字节） |
| 写回落在控制根 .agenticdoc 任务目录 | PASS：trace.log / output.md / worker.log / cwd-evidence.txt 全在 `<控制根>\.agenticdoc\mwtp-e2e-spawn\workers\mwtp-e2e-cwd-probe\` |
| 写回不落分片 | PASS：分片目录 spawn 前后均仅 README.md，无任何新增文件 |
| 撕裂未误判 | PASS：任务正常 running→done；task 目录无 "torn" 字样；trace.log 全程无 config torn 行 |
| （加分）active 切走后 spawn 被拒 | PASS：见下 |

### 6. 加分项：config tear 拒绝路径（真实观察）
第二探针 `mwtp-e2e-torn-probe`：task.md 同样真实注入 v2 块（mode: partition），随后 `mw partition off`（active: single，partition 块保留）再入队 pending。launcher（同参数第二实例）1s 内处理：
- 队列：pending→**failed**
- trace.log：`[LAUNCHER] 2026-09-19T05:52:45+00:00 config torn: task.md profile mode 'partition' != active mode 'single' (target.yml active switched after dispatch) — spawn refused; re-dispatch the task`
- worker.log：`[launcher] spawn failed (...): config torn: ...`
- **无** `[START] pid=` 行、无 pi 进程、无 cwd-evidence-torn.txt（spawn 在 Popen 之前被拒）
- 偏差说明：任务建议切 dual；本控制根无 dual 块（`mw target on` 需要 dual 块），改用 `mw partition off`（active→single）。撕裂判据是 `recorded != active`，dual/single 等价触发同一 fail-closed 路径。
- 测后已 `mw partition on` 恢复 active: partition（exit=0）

### 7. 清理
- launcher 1（Start-Process PID 99264）与 launcher 2（PID 45124）已按精确 PID Stop-Process 终止；PATH python 为 WindowsApps shim，真实解释器子进程（beat 文件 PID 9436/98736）复核均已死亡，系统内无残留 `--project mwtp-e2e-spawn` 的 launcher 进程（其余在跑 launcher 均属其他项目/会话，未触碰）
- 注入脚本 `mwtp-e2e-inject.mjs` 已删除（临时产物，不留仓库）
- %TEMP% 目录保留供复核：`mwtp-e2e-spawn\`（控制根，含全部证据文件）、`mwtp-e2e-parent\`、`mwtp-e2e-partition\`、`mwtp-e2e-sdk\`、`mwtp-e2e-logs\`
- 本仓库源码/test/其他 .agenticdoc 内容零改动（git status 仅原有会话改动，无本次新增）

## 证据文件清单（写回路径）
- `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-spawn\.agenticdoc\mwtp-e2e-spawn\workers\mwtp-e2e-cwd-probe\cwd-evidence.txt`
- `...\mwtp-e2e-cwd-probe\trace.log`（[START]/[MODEL]/[TOOL]/[END] 全锚点）
- `...\mwtp-e2e-cwd-probe\output.md`（TL;DR + Summary + Exit Reason: Agent settled after 3 tool call(s).）
- `...\mwtp-e2e-cwd-probe\worker.log`（[worker] done exit=0）
- `...\mwtp-e2e-torn-probe\trace.log` / `worker.log`（config torn 拒绝证据）
- `C:\Users\wenbozhou\AppData\Local\Temp\mwtp-e2e-logs\launcher{,2}-{out,err}.log`

## 分级
真实 spawn 完成（非部分/非受阻）。链路：mw partition set（真实 CLI）→ dist dispatchTask 注入（真实 bundle 代码）→ _workers.parallel 队列 → 独立 launcher 进程 → 真实 pi worker（timi/glm-5.3 直连，消耗 1 次模型调用）→ cwd=分片根 + 写回控制根 + tear 拒绝路径双验证。

# Review 记录：ai-baseline-repair（2026-09-10，verify 阶段）

> 审查对象：commit `04bc69616`（17 文件 +316/−78）+ 本 key 未提交文档面
> 方法：PM 直执 review（workflow-review 流程）；全文重读 diff 分 5 批（generator+provider / cross-api / tool-choice+stream+timi-dispatch / M1-M2 换 ID 五文件 / image-models+baseten+fireworks+copilot）

## 逐文件结论

| 文件 | 结论 |
|---|---|
| `scripts/generate-models.ts`（+37 镜像块） | 与上游结构一致；去重集合 `cloudflareAiGatewayModelIds` 先收原生条目再镜像，防重复合成；`tool_call !== true` 过滤与库「仅工具调用模型」约定一致 |
| `src/providers/cloudflare-ai-gateway.ts` | D-001 显式泛型 + 三 API pin 注释；功能保留（VC-004 过） |
| `src/image-models.generated.ts` | +214/−4 纯再生成漂移（新增 bytedance seedream 等；删除仅 4 行块边界），AGENTS.md 明确允许随提交 |
| `test/cross-api-thinking-history.test.ts` | 断言目标值全部未动：baseUrl 补缺、end_turn→stop（StopReason 枚举现实）、stop→done（流事件 union）、5 处 `as unknown as`（union 交叉必经）；never-窄化用局部变量重拓宽（VC-006 无断言删除 ✓） |
| `test/openai-completions-tool-choice.test.ts` | 第二 case grok-build-0.1→kimi-k2.6（镜像上游；catalog compat 实证见表）；compat 窄化 cast 带注释 |
| `test/stream.test.ts` | 仅 1 行 ID 换（claude-sonnet-4-5→4.5 点号形式）；曾误删 118 行已单文件还原，最终 diff 1 行 ✓ |
| `test/timi-dispatch.test.ts` | TS1294 参数属性→显式字段赋值（可擦语法要求），语义等价 |
| abort/total-tokens/context-overflow/tool-call-id-norm | 纯 M1/M2 换 ID 5 处，无逻辑改动 |
| baseten/fireworks/github-copilot/timi-models | 镜像上游（baseten 替换版/fireworks 删除/copilot gpt-5-mini）+ fork 特有对齐（timi glm-5.3）；见 S3-2 |
| README/CHANGELOG | 预防节 3 条规则 + Unreleased Fixed 3 条，位置正确（[Unreleased] 内） |

## 发现

- **S1（阻断）：无**
- **S2（应修）：无**
- **S3（观察，不动作）**：
  1. tool-choice 的 `as OpenAICompletionsCompat | undefined` 直接 cast——union 类型面窄化必经，D-005 已记录替代方案（改 getModel 泛型）成本更高；
  2. baseten 测试名随本地目录现实改为 "multimodal"（上游 09-03 为 text-only）——models.dev 输入会再漂，属目录 churn 固有，README 卫生节已立规矩；
  3. cross-api 的 5 处双重 cast 是 ResponseInputItem union 的类型债，若上游未来统一类型面可顺带消除；
  4. `cloudflare-ai-gateway.models.ts` 为 gitignored 生成物（与 data json 同源），镜像块经再生成自动流入，无手工产物遗漏。

## 结论

**LGTM**。无阻断/应修发现；S3 四项均已有留痕或属固有漂移。代码质量门（tsgo 0 错 + biome 零告警 + 套件 838 过）已在 T-04 验证链覆盖。

# L2 Smoke Run: AC-015/AC-016 — 2026-09-09

## Setup

- Task: `smoke-ac015-ac016`（mw-dispatch-flow-fixes/workers/，type: coding，pi/timi 默认路由）
- mw serve PID 62804 运行中；bundle 已重建安装（mw build --install，activate self-check OK）
- 队列入队走真实 WorkerStore（锁协议合规）

## 结果

| 断言 | 结果 | 证据 |
|------|------|------|
| 队列 pending → running → done | PASS | `_workers.parallel` 行 `smoke-ac015-ac016 \| done \| pi \| timi \| ...` |
| trace.log 结构化生命周期行 | PASS | `[START] 2026-09-09T04:09:09.314Z task=smoke-ac015-ac016 type=coding phases=-` + `[END] ... exit=0 elapsed=2s tools=0 phases=-` |
| worker.log 状态行 | PASS | `[worker] start ...` + `[worker] done exit=0 elapsed=2s tools=0`（fs.writeSync 同步落地） |
| output.md 四节 | PASS | Summary: SMOKE-OK-AC016，exitReason: Agent settled after 0 tool call(s) |
| worker.log 含 pi 最终回复 | **FAIL（被阻塞）** | 只有 [worker] 状态行；最终回复在 print-mode 写出前进程已 exit(0) |

## FAIL 根因（preload --require 钩子抓栈）

```
process.exit(0) called
    at exitWithSuccess (packages/coding-agent/dist/extensions/agent-team-loop/worker/worker-mode.js:125:17)
    at ExtensionRunner.emit (dist/core/extensions/runner.js:588:49)
    at async AgentSession._emitAgentSettled (dist/core/agent-session.js:330:13)
    ...
```

- 本 fork 将 agent-team-loop 注册为 **built-in 扩展**（`src/extensions/index.ts`，初始提交 8806e2be），编译进 `packages/coding-agent/dist/`，**最后构建于 2026-08-14**。
- npm 全局 `pi.cmd` 指向本仓 `packages/coding-agent/dist/cli.js` → 每个 pi 进程同时加载：
  1. stale built-in（08-14，无 ACTIVATION_FLAG 双载防护 → 必激活）
  2. 全局 bundle（当日新构建）
- 旧 built-in 的 `exitWithSuccess` 在 agent_settled 内 `process.exit(0)`，先于 print-mode 的最终回复写出 → **worker.log 空的真根因**（与 src 侧同形缺陷叠加：src 旧代码也 exit，已在本 key 修复，但 dist 未重建所以旧行为仍在跑）。
- 同一 stale 副本的 PM poll 循环无 session_shutdown 清理 → 裸 `pi -p "Say exactly: hello"` 实测：回复已写出（"hello"）但进程挂死不退出（180s 超时强杀）。无扩展环境（空 PI_CODING_AGENT_DIR）因无凭证快速失败，可区分。

## 修复决策（已定：A）

- A) 保留 built-in + 重建 dist（日常改扩展仍只需 `mw build --install`：全局 bundle 先加载并设 ACTIVATION_FLAG，重建后的 built-in 让位为惰性后备，仅全局 bundle 缺失时接管）
- B) 从 builtInExtensions 移除 agent-team-loop（未采纳）

## 修复后复验（smoke-ac016b，dist 重建 2026-09-09 14:43 后）

- 派发：task.md 落盘 → PM after-turn 未派发扫描 → 项目级队列 pending → Python spawn → done（06:45:54 → 06:46:10）
- **worker.log：`[worker] start ...` + `[worker] done exit=0 elapsed=12s tools=0` + 最终回复 `SMOKE-OK-AC016B` 三段俱全 —— AC-016 PASS**
- trace.log：`[START] 06:45:56.567Z task=smoke-ac016b type=default phases=-` + `[END] 06:46:08.076Z exit=0 elapsed=12s tools=0 phases=-`
- 裸 `pi -p "Say exactly: ok"`：输出 ok 且 **4.1s 自然退出**（此前 180s+ 挂死需强杀）—— stale built-in PM poll 循环挂死问题随 dist 重建消除
- 附带发现①：运行中的 mw serve（PID 62804，8-28 启动）携带的是 **9-5 之前的 launcher**：任务体仍整体经 argv 传给 `-p`（会话文件证实 user message = task.md 全文，worker 0 工具调用即答）；这正是 9-5 "task body never travels through argv" 修复前的行为（cmd.exe `%*` 重分词风险）。建议择机 `mw stop && mw start` 重启以生效新 launcher；注意 mw.py/mw_common.py 有另一会话的未提交改动，重启需用户确认
- 附带发现②：agent-team-loop 扩展只读写项目级 `.agenticdoc/_workers.parallel`（WorkerStore 恒以项目根构造）；烟测脚本误写的 key 级队列文件无人读取，已持锁清空

## 复现材料（已清理）

tmp/ 下的 preload 钩子、spawn 复现脚本、临时日志均已删除；smoke 任务目录保留于 `.agenticdoc/mw-dispatch-flow-fixes/workers/{smoke-ac015-ac016, smoke-ac016b}/`。
